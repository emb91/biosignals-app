---
description: Git workflow - always use feature branches, never push directly to main
alwaysApply: true
---

# Git Workflow

## Never Push Directly to Main

Always use feature branches and pull requests. Never commit or push directly to the `main` branch.

## Workflow

1. **Create a feature branch** before making changes:
   ```bash
   git checkout -b feature/descriptive-name
   ```

2. **Make commits** on the feature branch

3. **Push the branch** and create a PR:
   ```bash
   git push -u origin HEAD
   gh pr create --title "Title" --body "Description"
   ```

4. **Merge via PR** after review

## Branch Naming

Use prefixes:
- `feature/` - new features
- `fix/` - bug fixes
- `refactor/` - code refactoring
- `chore/` - maintenance tasks

Examples:
- `feature/company-analysis-updates`
- `fix/login-redirect`
- `refactor/sidebar-component`
